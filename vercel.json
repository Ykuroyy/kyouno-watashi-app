{
  "buildCommand": "cd StrengthMapApp && npm install --legacy-peer-deps && npm run build:web",
  "outputDirectory": "StrengthMapApp/dist",
  "installCommand": "echo 'Skip install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}